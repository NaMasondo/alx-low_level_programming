#include <stdio.h>

/**
 * main - "prints a string using printf function followed by a line"
 *
 * Return: Always 0.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
