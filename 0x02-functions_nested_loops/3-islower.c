#include "main.h"
/**
 * _islower - "checks for lowercase character"
 *
 * Return: 1 If "c" is lowercase and 0 otherwise.
 */

int _islower(int c)
{
	char c = 97;

	if (c <= 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
