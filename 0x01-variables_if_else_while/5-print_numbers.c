#include <stdio.h>

/**
 * main - "prints all single digit numbers from 0"
 *
 * Return: Always 0.
 */
int main(void)
{
	int x;

	for (x = 0; x <= 9; x++)
	{
		printf("%d", x);
	}
	printf("\n");
	return (0);
}
